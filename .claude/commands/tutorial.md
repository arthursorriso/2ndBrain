---
description: Create a step-by-step tutorial/runbook
---

Create a tutorial about: "$ARGUMENTS"

1. If the user provided the steps/commands, structure them with `templates/tutorial.md` in `40-tutorials/<slug>.md`.
2. If not, ask: what's the goal, the steps/commands, prerequisites, and common errors.
3. Commands always in code blocks with the right language. Numbered steps, one goal per step.
4. If a tutorial on the topic already exists, update it (and its `updated` field) instead of creating another.
5. Link related projects.
