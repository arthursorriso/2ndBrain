---
description: Search the vault and answer with sources (total memory)
---

Answer using the vault as memory: "$ARGUMENTS"

1. Grep the entire vault. Prioritize by question type: conceptual ("what is", "how does it work") → `70-knowledge/` · historical ("what was decided", "who said") → `30-meetings/` (including `raw/`), dailies, and `90-memory/decisions.md`. Always include the `raw/` folders — they hold what summaries cut.
2. Read the relevant notes before answering.
3. Answer directly and ALWAYS cite sources: `[[note]]` + date.
4. If sources diverge, show the versions and dates — the most recent prevails. If a knowledge note has a recorded contradiction, mention it.
5. If nothing is found, say clearly it isn't recorded in the vault. NEVER make things up.
