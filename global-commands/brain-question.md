---
description: Query your second brain's memory (from anywhere)
---

Answer using the second brain (vault at `~/2ndBrain`) as memory: "$ARGUMENTS"

1. Grep all of `~/2ndBrain/`, including `30-meetings/raw/` (raw transcripts hold what summaries cut).
2. Read the relevant notes before answering.
3. Answer directly and ALWAYS cite sources: note name + date.
4. If sources diverge, show the versions and dates — the most recent prevails.
5. If nothing is found, say clearly it isn't recorded in the vault. NEVER invent facts, dates, names, or decisions.
6. Do not modify any vault file — this command is read-only.
